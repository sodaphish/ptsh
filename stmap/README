`stmap` is an XML-based software publishing -- it allows you to create a 
web-page for each project based off of an XML file in each project's and 
also publishes releases of the project to a common location.
